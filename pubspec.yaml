name: health_app_ui
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  device_preview: ^1.1.0
  ionicons: ^0.2.2
  google_fonts: ^5.1.0

flutter:
  uses-material-design: true

  assets:
    - assets/
  
